# OSINT Baseline

The initial OSINT integration for EYERIS is intentionally minimal.

Uploaded launcher baseline:

```bash
python tracker.py
```

Repository wrapper:

```bash
bash osint/run_tracker.sh
```

This establishes the OSINT entry-point convention only. A future `tracker.py` implementation must be added and tested before the tracker can be considered operational.
